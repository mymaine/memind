# Documentation — most of docs/ ships with the repo (English for reviewers).
# The paths below stay local:
#   spec.md       — internal roadmap, not public
#   product.md    — superseded by README Problem / What we built sections
#   conventions.md — internal naming + git rules
#   archive/      — historical drafts, superseded
#   features/     — placeholder, empty
#   runbooks/     — internal ops drafts
docs/spec.md
docs/product.md
docs/conventions.md
docs/archive/
docs/features/
docs/runbooks/

# Node / TypeScript
node_modules/
dist/
build/
.next/
out/
coverage/
*.tsbuildinfo

# Env / secrets
.env
.env.local
.env.*.local
*.key
*.pem

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# rules
AGENTS.md
CLAUDE.md

# runtime artifacts (generated meme images from demo runs)
apps/server/tmp/